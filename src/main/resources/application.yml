server:
  port: 8085

spring:
  application:
    name: statementservice

cloud:
  aws:
    credentials:
      accessKey:
      secretKey:

    region:
      static: us-east-2
    s3:
      bucket: s3test-bna

    streamname: TestStream
    statementStream: stmStream
